# CI + Cloud Reports (Allure + GitHub Pages)

This template includes `.github/workflows/ci.yml` that:
1) Runs tests
2) Generates Allure report
3) Publishes to `gh-pages`

## Enable Pages
GitHub repo → Settings → Pages → Deploy from branch → `gh-pages` / root

After the first run, open the report URL.
